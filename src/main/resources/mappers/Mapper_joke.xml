<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 연결할 인터페이스 경로를 namespace에 작성 -->
<mapper namespace="com.yeonfish.multitool.mappers.Mapper_joke">
    <select id="get" resultType="String" parameterType="String">
        select
            *
        from
            spring.MTT_joke
        where
            `id` = #{id}
    </select>
    <select id="set" resultType="int" parameterType="String">
        insert into spring.MTT_joke (`id`) values (#{id})
    </select>
    <delete id="del" parameterType="String">
        delete from spring.MTT_joke where `id` = #{id}
    </delete>
</mapper>